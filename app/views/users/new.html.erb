<%= form_for @user, url: {action: "create"}, html: {class: "content"} do |f| %>
	<%= f.label :email %>
	<%= f.email_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit 'Create user' %>
<% end  %>
