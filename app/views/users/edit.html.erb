<%= form_for @user, url: {action: "update"},
	html: {class: "content"}, method: :patch do |f| %>
	<%= f.label :email %>
	<%= f.email_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit 'Update user' %>
<% end  %>
