<%= form_for @event, url: {action: "update"},
	html: {class: "content"}, method: :patch do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :disc_jockey %>
	<%= f.text_field :disc_jockey %>
	
	<%= f.label :description %>
	<%= f.text_area :description %>
	
	<%= f.label :start %>
	<%= f.datetime_select :start %>
	
	<%= f.label :finish %>
	<%= f.datetime_select :finish %>
	
	<%= f.label :location %>
	<%= f.text_area :location %>

	<%= f.submit 'Update event' %>
<% end  %>
