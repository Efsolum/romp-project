<%= form_for @event, url: {action: "destroy"},
	html: {class: "content", method: :delete} do |f| %>
	<p>
		Are you sure you wish to delete event <%= @event.name %>.
	</p>

	<%= link_to 'No', :back %>
	<%= f.submit 'Delete event' %>
<% end  %>
